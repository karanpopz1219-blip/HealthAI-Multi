# Core dependencies (pinned)
streamlit==1.30.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.3.2
matplotlib==3.8.1
seaborn==0.13.2
fastapi==0.95.2
uvicorn[standard]==0.23.1
joblib==1.3.2

# Optional / heavy ML dependencies (install if you need full functionality)
# Install these only if you need full features (large downloads):
# torch==2.2.0
# transformers==4.35.0
# tensorflow==2.13.0
# mlxtend==0.21.0
# sentencepiece==0.1.99

# Explainability / interpretability (optional)
# shap==0.41.0
# lime==0.2.2.0

# Note: On macOS, prefer conda for TensorFlow to avoid binary issues.
